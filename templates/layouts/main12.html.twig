<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome to project!{% endblock %}</title>
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}

        <!--
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
        -->
        
        <link rel="shortcut icon" href="{{ absolute_url('images/favicon.jpg') }}" />

        <script src="/build/runtime.js" defer></script>
        <script src="/build/app.js" defer></script>

        <link rel="stylesheet" type="text/css" href="/build/app.css" />
    </head>
    <body>

        <div class="container">
            <div class="row">
                <div class="col-md-4 text-left">
                    <div style="margin-top:15px;">
                        <a href="{{ path('app_subscribe') }}" class="a_subscribe">Subscribirse</a>
                    </div>
                </div>    
                <div class="col-md-4 text-center">
                    <a href="{{ path('index') }}" style="color:#198754; font-size:40px; text-decoration:none;">MiHospital</a>
                </div>
                <div class="col-md-4 text-right">
                    <div style="margin-top:10px;">
                        <a href="{{ path('login') }}" class="btn btn-outline-success">Entrar</a> 
                        <a href="{{ path('register') }}" class="btn btn-success">Registrarse</a>
                    </div>    
                </div>
            </div>
            <div class="row">
                <div class="col-md-12 line-menu">

                    <div class="div-menu">
                        <a href="{{ path('paciente_index') }}" class="a_menu">Pacientes</a>
                    </div>
                    <div class="div-menu">
                        <a href="{{ path('doctor_index') }}" class="a_menu">Doctors</a>
                    </div>
                    <div class="div-menu">
                        <a href="{{ path('animals_index') }}" class="a_menu">Animales</a>
                    </div>
                    <div class="div-menu">
                        <a href="{{ path('lucky') }}" class="a_menu">Lucky</a>
                    </div>
                    <div class="div-menu">
                        <a href="{{ path('index') }}" class="a_menu">Artículos</a>
                    </div>
                    <div class="div-menu">
                        <a href="{{ path('index') }}" class="a_menu">Comentarios</a>
                    </div>
                    <div class="div-menu">
                        <a href="{{ path('map') }}" class="a_menu">Map</a>
                    </div>
                    
                </div>    
            </div>
            
            {% block body %}{% endblock %}

        </div>
    </body>
</html>